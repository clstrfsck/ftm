//! Lock down: extended placement and its variants (§9.11).

// TODO(stage 4): extended placement with the 15-reset cap and the
// `lowest_row_reached` rule, plus the `infinite` and `classic` variants.
