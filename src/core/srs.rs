//! Super Rotation System: wall-kick tables and rotation resolution (§9.5).
//!
//! **The kick tables in §9.5 are already converted to the y-down convention of
//! §5. Transcribe them as written; do not negate them again.**

// TODO(stage 2): both kick tables, `try_rotate` returning the new origin *and*
// the kick index used (§9.13 needs the index), and the 180-degree path, which
// takes no kick tests.
