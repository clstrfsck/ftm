//! Falling Tetromino Manager — the window front-end's entry point.
//!
//! A stub. The target, its feature and its `[[bin]]` are wired before there is
//! anything behind them, so `EGUI.md` stage G5 adds a front-end rather than a
//! build system (G2).

#![forbid(unsafe_code)]

fn main() {
    eprintln!("ftm-gui: not built yet (EGUI.md stage G5)");
}
