//! Configuration: `RulesConfig` and `PresentationConfig`, TOML load/save and the
//! CLI merge (§6).
//!
//! The two structs stay separate (§6.5): only `RulesConfig` affects the core.

// TODO(stage 3): the two structs, §6.3 defaults, `ms_to_ticks` (§6.6) and the
// tick constants of §15.1.
// TODO(stage 10): TOML load/save at the §6.2 path and the §6.4 CLI merge.
